// Default (development) environment. Swapped for environment.prod.ts in
// production builds via the fileReplacements in angular.json.
export const environment = {
  apiUrl: 'http://localhost:5080'
};
