// Same value as the dev environment for this take-home POC (single backend,
// no separate production deployment). Kept as its own file to follow Angular's
// standard fileReplacements convention, so a real prod URL is a one-line change.
export const environment = {
  apiUrl: 'http://localhost:5080'
};
